### Task ID: 06
### Title: Create CLI Entry Point for Memory Agent

**Description:**

Create a new user-facing script in the `bin/` directory to allow users to launch the new `MemoryAgent` from the command line.

**Acceptance Criteria:**

- [ ] A new executable file `bin/memory-agent` is created.
- [ ] The script correctly initializes and runs the `MemoryAgent::AgentManager`.
- [ ] The script accepts a topic as a command-line argument.

**Dependencies:**

- Task 05: Integrate Memory into Agent's Decision Loop

**Files to be Created/Modified:**

- `bin/memory-agent`
